include: package:flutter_lints/flutter.yaml

# Regole di analisi statica per mantenere il codice idiomatico e leggibile.
analyzer:
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    - prefer_final_locals
    - prefer_const_constructors
    - avoid_print
